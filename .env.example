# Supabase — auth (accounts) + Postgres + RLS
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Groq API key for the /api/funnel-analyze endpoint (AI Analyze mode).
GROQ_API_KEY=

# Optional: only needed for server-side admin ops that bypass RLS (not used yet).
# SUPABASE_SERVICE_ROLE_KEY=
