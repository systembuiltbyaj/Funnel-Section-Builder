# OS Audit — 2026-08-08

**Knowledge current through: 2026-08-07 14:26** — the last commit touching any doc
(`README.md`, `SUPABASE_SETUP.md`, `CLAUDE.md`, all three at 14:26). Code moved at
14:58 and again at 16:34+. Your operating manual predates the refactor it describes.

Scope: `(B) Real Apps/Funnel Builder` project root. Application repo, no recurring data
feeds — Check 3 is reframed around the docs layer. Read-only: nothing was changed.

| Check | Verdict | Worst finding |
|---|---|---|
| Routing integrity  | RED | `CLAUDE.md` routes the section catalog to `app/private-content.tsx, const sections` — that symbol moved to `lib/section-catalogue.ts:55` |
| Index truth        | YELLOW | Sample registry is a perfect 100/100 match; `empathy-v9`/`v10` still have thumbnails with no sample |
| Freshness          | RED | All three docs are pinned at 14:26; the catalogue extraction (14:58) and hub flow (16:34) landed after |
| Bloat/duplication  | YELLOW | Catalogue bloat moved rather than shrank — `lib/section-catalogue.ts` is 11,683 lines |
| Hygiene            | YELLOW | Secrets clean, 33 tests now exist; still zero API-route/RLS tests and no hooks or CI |
| Context placement  | RED | The always-loaded manual carries a stale structural fact; `/review` and `/sections` both claim `step={2}` |

## Failure-mode exposure

| Mode | Exposure | Driven by |
|---|---|---|
| Poisoning (false)            | HIGH | `CLAUDE.md` misroutes the catalog and states a 13k line count that is now 1,134 |
| Bloat (too much)             | MED  | 11,683-line `section-catalogue.ts`; 51 static `gpt-img-*` entries carried in the catalogue |
| Confusion (wrong or missing) | HIGH | 11 new modules/routes unmapped; the entire global `System/` build-system route tree is gone |
| Clash (contradictory)        | HIGH | Two pages both labelled step 2; `/build` is step 3 but drops the stepper entirely |

## Since last audit (2026-08-07)

**Fixed — genuinely good work:**
- Phantom asset `/private/hero-v9-person.webp` is gone (reference removed, not faked).
- **The sample registry is now exact**: 100 `FLAT_SAMPLES` entries ↔ 100 `-sample.html` files on
  disk, zero phantoms, zero orphans. All 16 `NESTED_SAMPLES` targets resolve. All 172 `previewSrc`
  thumbnails exist. Yesterday's "135 orphans" finding is fully closed — the sample-preview feature
  was wired, which answers audit question 1.
- `README.md` and `SUPABASE_SETUP.md` rewritten and now accurate. Both "Not yet wired" claims
  verified true (nothing writes `analysis_history`; no RLS test exists).
- Dev port fixed to 3100 in `package.json`, README, and the Supabase instruction — all three agree.
- `typecheck` and `test` scripts added; 5 test files, 33 passing tests.
- A project `CLAUDE.md` now exists **with a precedence rule** ("the code wins and the doc is the bug").
- `app/private-content.tsx`: 13,076 → 1,134 lines.

**Worse:**
- Global `CLAUDE.md`'s Build System block: yesterday's report said the four `core/*.md` targets
  resolved. Today **the entire `System/` folder does not exist** — `System-BuiltBy AJ/` contains
  only `Projects/`. All five routes are dead (see Check 1).

**New:**
- The manual went stale within 32 minutes of being written, in the same way the README did
  yesterday — and this time the stale file is the one loaded into every session.

## What would make your agent wrong-answer you today

1. **Ask "where is the section catalog?"** → `CLAUDE.md`'s routing table sends it to
   `app/private-content.tsx`, `const sections: Section[]`. That symbol is not in that file
   anymore; it lives at [lib/section-catalogue.ts:55](lib/section-catalogue.ts#L55). The file
   it points at *does* exist, so nothing errors — the agent opens it, finds no catalog, and
   improvises. This is the worst failure shape in the audit.
2. **Ask "add a new variation."** → The manual says the catalogue is in a 13k-line file and to
   read a narrow line range. The real file is 1,134 lines and the real catalogue is elsewhere.
   The agent budgets its reading against a file that no longer matches the description.
3. **Ask "what does this project's `lib/` do?"** → Six modules carrying the app's core logic
   (`section-catalogue`, `prompt-assembly`, `funnel-selection`, `analyze`, `catalogue`,
   `prompt-groups`) appear nowhere in the manual. Neither do `/build`, `/review`, `/sections`,
   `/reset`, or `app/hub/`.
4. **Ask for the build system or frontend guidelines** → global `CLAUDE.md` routes to
   `…/System-BuiltBy AJ/System/…`. That folder is gone. Five dead routes, no error.

## Findings by check

### Check 1 — Routing integrity — RED

- **Misroute (worst kind).** `CLAUDE.md`'s "Where things live" table maps *Section catalog — every
  variation, its prompt, its thumbnail* → `app/private-content.tsx`, `const sections: Section[]`.
  Verified: `grep "const sections" app/private-content.tsx` → no match. The declaration is at
  `lib/section-catalogue.ts:55`. Cause: `CLAUDE.md` was committed 14:26; the extraction commit
  `cfa3d34` landed 14:58. `[poisoning]`
- Same table's note — *"`app/private-content.tsx` is ~13k lines, nearly all of it prompt text inside
  the `sections` array. Read the specific line range you need"* — is now false on every clause.
  Measured: 1,134 lines, no `sections` array. `[poisoning]`
- **Unmapped and therefore invisible to a fresh session** — all real, all active:
  `lib/section-catalogue.ts` (11,683 lines), `lib/prompt-assembly.ts`, `lib/funnel-selection.ts`,
  `lib/funnel-selection-provider.tsx`, `lib/analyze.ts`, `lib/catalogue.ts`, `lib/prompt-groups.ts`,
  `app/hub/` (5 components), and routes `/build`, `/review`, `/sections`, `/reset`. `[confusion]`
- **Global manual, Build System block — all five routes dead.** `System/` does not exist under
  `System-BuiltBy AJ/`. The four `core/*.md` files do exist, but scattered and outside any `System/`:
  | Referenced as | Actually at |
  |---|---|
  | `System/core/AGENT_PLAYBOOK.md` | `Projects/System-BuiltBy AJ/Z. MD Files/AGENT_PLAYBOOK.md` |
  | `System/core/APP_BLUEPRINT.md` | `Projects/System-BuiltBy AJ/Z. MD Files/APP_BLUEPRINT.md` |
  | `System/core/AI_AGENT_SYSTEM.md` | `Projects/Side-HustleBy AJ/(A) Main Funnel/AI_AGENT_SYSTEM.md` |
  | `System/core/BUILD_SYSTEM.md` | `Projects/Side-HustleBy AJ/(A) Main Funnel/BUILD_SYSTEM.md` |
  | `System/frontend/frontend-guidelines.md` | **nowhere** — no such file under `Claude Code/` |
  Two of the four sit inside an unrelated project (`Side-HustleBy AJ`), which is its own clash:
  a shared build kit living in one project's folder. `[confusion]` `[clash]`
- No `.claude/` directory in this project — no project-scoped skills or agents to spot-check.
- No `MEMORY.md` — nothing carrying "the catalogue moved to `lib/`" forward between sessions.

### Check 2 — Index truth — YELLOW

The registry↔disk reconciliation is **clean in both directions**:

| Direction | Result |
|---|---|
| `FLAT_SAMPLES` entries | 100 |
| `*-sample.html` on disk | 100 |
| Phantom (registered, no file) | **0** |
| Orphan (file, unregistered) | **0** |
| `NESTED_SAMPLES` targets resolving | **16 / 16** |
| `previewSrc` thumbnails on disk | **172 / 172** |

This is the check most likely to fail silently — `CLAUDE.md` itself warns the preview "silently
falls back to the static thumbnail with no error" — and it passes exactly.

- 56 of 172 variations return `null` from `sampleForPreview()`. 51 are `gpt-img-*` (image-prompt
  library) and 3 are `layout-*` screenshots; the docstring documents both as intentional. **But
  `empathy-v9` and `empathy-v10` are ordinary sections with thumbnails and no sample** — they
  degrade silently with nothing explaining why. Carried over unfixed from yesterday. `[confusion]`
- `before-after-v7` still absent entirely (v1–v6, v8 present). Carried over. `[confusion]`
- Two of five `layout-*` entries (`lunara`, `isla-serena`) *do* have samples while
  `layout-3-kampo` and the two `layout-muni-*` don't — inconsistent within one group. `[clash]`

### Check 3 — Freshness (docs vs. code) — RED

| Artifact | Last commit | Verdict | What's wrong |
|---|---|---|---|
| `CLAUDE.md` | 2026-08-07 14:26 | **FROZEN** | Predates `cfa3d34` (14:58); catalog route and file-size note both wrong |
| `README.md` | 2026-08-07 14:26 | **DRIFTING** | Accurate on auth/env/stack; describes "Three modes", never mentions the three-step stepper |
| `SUPABASE_SETUP.md` | 2026-08-07 14:26 | **FRESH** | Verified accurate, including both "Not yet wired" items |
| `.env.example` | — | **FRESH** | Matches the three vars set in Vercel |
| Sample registry | — | **FRESH** | 100/100 exact |

- Yesterday's headline was "docs froze two commits behind code within a single day." That was fixed,
  then **recurred within 32 minutes** — same mechanism, higher-stakes file. There is still no hook,
  CI job, or checklist coupling doc updates to code changes, which is the actual root cause. `[clash]`
- README's "Three modes: Analyze Copy (AI) / Manual / Brand Check" describes the old single-page
  builder. `app/page.tsx` now renders `HubShell step={1}`; the app is a stepped flow with saved
  funnels. Not wrong so much as describing a shape the app has outgrown. `[poisoning]`

### Check 4 — Bloat, duplication, organization — YELLOW

- **The single biggest win since yesterday**: `app/private-content.tsx` 13,076 → 1,134 lines.
- **But the bloat relocated rather than resolved**: `lib/section-catalogue.ts` is 11,683 lines,
  still 72% of the 16,182-line codebase. It is genuinely better — JSX-free, snapshot-tested, not
  always-loaded — but any agent asked to touch a variation still faces an 11.7k-line file with no
  per-section index. `[bloat]`
- **Root hygiene: clean.** All 15 loose root files are legitimate Next.js config, docs, or
  lockfiles. No scratch, no dumps, no stray exports. Nothing to re-home.
- `.superpowers/` (brainstorm mockups, `.last-token`, task briefs, review diffs) is correctly
  gitignored at `.gitignore:35` and has **0 tracked files** — scratch is properly quarantined
  from the knowledge tree.
- `docs/superpowers/` (plan + spec) and `audits/` are tracked, which is right — they're durable
  decisions, not scratch.
- No empty directories, no 0-byte files.
- **Intuitiveness gut check** — walked three artifacts by folder name alone:
  - *"the empathy v3 live preview"* → `public/private/empathy-v3-sample.html`. Found, no search.
  - *"where the auth screen lives"* → `app/auth-gate.tsx`. Found.
  - *"where variation metadata is defined"* → **failed.** The manual sends you to
    `app/private-content.tsx`; the answer is `lib/section-catalogue.ts`. A human following the
    documented trail lands in the wrong file. Yesterday this trail was merely long; today it is wrong.

### Check 5 — Hygiene and silent failures — YELLOW

- **Secrets: PASS.** `.env.local` matched by `.gitignore:24` and untracked (`git check-ignore -v`
  confirms). Only `.env.example` is tracked, placeholder names only. 375 tracked files, no
  credential files, no PII, no exported data. `.vercel` is gitignored ahead of need.
- **Tests now exist** — 5 files, 33 passing: `funnel-selection`, `prompt-assembly` (+ fixture
  snapshot), `samples`, `section-catalogue`. Yesterday this was zero. Typecheck, lint, test, and
  build all pass on `main` as of this audit.
- **Coverage gap is real and honestly documented.** No test touches `app/api/projects`,
  `app/api/projects/[id]`, or `funnel-analyze`. RLS scoping (user B cannot read user A's projects)
  remains unverified — `SUPABASE_SETUP.md:55` states this plainly, which is the right behaviour
  for a doc. Your global rules require a unit test per service function; the API layer has none. `[confusion]`
- **No hooks, no CI, no scheduled jobs, no `.claude/` directory.** Every gate is manual. This is
  the direct cause of the Check 3 recurrence: the merge to `main` ran four gates but nothing
  checked whether `CLAUDE.md` still described the code. `[clash]`

### Check 6 — Context placement — RED

- **Situational fact baked into the expertise layer.** `CLAUDE.md` is loaded into every session and
  states a file location and a line count — both structural facts with a shelf life, both now false.
  This is the double finding the check is designed to catch: guaranteed poisoning in the file the
  agent trusts most, plus per-session cost. The fix is a pointer that survives refactors
  ("the catalogue is the `sections` array — grep for `export const sections`"), not a fresher path. `[poisoning]` `[bloat]`
- **The precedence rule now exists and is well-written** — *"the code wins and the doc is the bug —
  fix it in the same change."* Genuine improvement over yesterday's total absence. The irony is
  that `CLAUDE.md` is now the doc that's wrong, and the same-change clause wasn't honoured for the
  extraction commit. The rule is sound; enforcement is missing.
- **Live clash inside the code itself**: `app/review/page.tsx:21` and `app/sections/page.tsx:22`
  both pass `step={2}` to `HubShell`, whose `STEPS` table
  ([app/hub/shell.tsx:6-10](app/hub/shell.tsx#L6-L10)) defines step 2 as "Review picks" → `/review`.
  So `/sections` ("Choose your sections") renders a stepper highlighting a different page's label.
  Separately, `/build` is step 3 in that table but [app/build/page.tsx:17](app/build/page.tsx#L17)
  renders `<PrivateContent />` bare — the stepper disappears at the final step. Both routes are
  reachable (`start.tsx:203`, `review-picks.tsx:43,166,180`). `[clash]`
- Spot-checked five facts across stores: *auth mechanism* → 3 stores, consistent ✅.
  *Env var list* → 3 stores, consistent ✅. *Dev port 3100* → 3 stores, consistent ✅.
  *Catalogue location* → 2 stores, **contradictory** ❌. *Flow step count* → 2 stores,
  **contradictory** ❌. Three of five clean, up from one of four yesterday.

## Questions for you

1. **`empathy-v9` / `empathy-v10` and `before-after-v7`** — flagged yesterday, still open. Are these
   intentional gaps, or catalogue entries waiting on sample HTML? One line in `CLAUDE.md` either way
   stops this recurring in every audit.
2. **The three `layout-*` entries without samples** (`kampo`, both `muni`) — deliberate, given the
   other two layouts have them?
3. **Where did `System/` go?** The global manual's entire Build System block points at it. The four
   `core/*.md` files exist but two are inside `Side-HustleBy AJ/(A) Main Funnel/`. Did you
   reorganise, or was `System/` never built as a folder? This is global, so it breaks every project.
4. **Did `System/frontend/frontend-guidelines.md` ever exist?** Nothing by that name exists anywhere
   under `Claude Code/`. Flagged yesterday, still unanswered — and you have frontend work queued.

## Fix list (batched, await approval)

**Batch A — security + dead capabilities (do first)**
- Nothing security-critical. Secrets handling is clean; no action needed.
- Repoint or remove the global `CLAUDE.md` Build System block: five dead paths, one of which
  (`frontend-guidelines.md`) has no target anywhere. Decide whether to recreate `System/` or
  rewrite the routes to where the files actually sit.

**Batch B — routing + index reconciliation (highest value, ~15 min)**
- Fix the `CLAUDE.md` "Where things live" table: catalog → `lib/section-catalogue.ts`; correct the
  `private-content.tsx` line-count note; add rows for `lib/prompt-assembly.ts`,
  `lib/funnel-selection*.ts`, `lib/analyze.ts`, `app/hub/`, and the `/sections` `/review` `/build`
  `/reset` routes.
- Phrase the catalog pointer refactor-proof (`export const sections`, not a line number).
- Update README's "Three modes" to describe the three-step flow.

**Batch C — code clash + catalogue gaps**
- Resolve the step numbering: give `/sections` its own step, or fold it under a shared label; make
  `/build` render inside `HubShell` so step 3 keeps the stepper.
- Answer questions 1–2 and either fill `empathy-v9`/`v10` + `before-after-v7` or document them.

**Batch D — durability (so the manual can't silently freeze a third time)**
- Add API-route and RLS tests (user B cannot read user A's projects) — the one materially
  unverified security behaviour in the app.
- Add a PostToolUse or pre-commit hook: when `lib/*.ts` or `app/**/page.tsx` are added/moved/deleted,
  flag `CLAUDE.md` for review. Twice in two days is a pattern, not an accident.
