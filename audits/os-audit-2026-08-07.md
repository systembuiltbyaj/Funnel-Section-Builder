# OS Audit — 2026-08-07

**Knowledge current through: 2026-08-07 (code) / 2026-08-07 08:47 (docs — 2 commits behind code)**

Scope: `(B) Funnel Section Builder` project root. This is an application repo, not a
knowledge base — there are no recurring data feeds, so Check 3 is reframed around the
template asset library and the docs layer. Read-only: nothing was changed.

| Check | Verdict | Worst finding |
|---|---|---|
| Routing integrity  | YELLOW | Global CLAUDE.md routes "frontend visual craft" to `System/frontend/frontend-guidelines.md` — the whole `frontend/` folder does not exist |
| Index truth        | RED | `/private/hero-v9-person.webp` is referenced by the builder but does not exist on disk — broken image in the UI |
| Freshness          | RED | README.md + SUPABASE_SETUP.md still describe the passcode gate that commit `08b26d5` deleted today |
| Bloat/duplication  | YELLOW | `app/private-content.tsx` is 13,076 lines (95% of all app code); 111 shipped assets are never referenced |
| Hygiene            | YELLOW | Zero tests, no `test`/`typecheck` npm script — global rules require both before "done" |
| Context placement  | YELLOW | No project-level CLAUDE.md; the 10P section catalog + prompt templates live only inside a 13k-line component |

Secrets check passed cleanly: `.env.local` is gitignored and untracked, `.env.example`
holds placeholder names only, no service-role key is committed anywhere.

## Failure-mode exposure

| Mode | Exposure | Driven by |
|---|---|---|
| Poisoning (false)            | HIGH | README env table lists two env vars that no longer exist; SUPABASE_SETUP.md's "still to wire" list describes work already shipped |
| Bloat (too much)             | MED  | 13,076-line single component; 111 unreferenced assets in `public/private`; 247 MB `.next` |
| Confusion (wrong or missing) | MED  | Dead `frontend-guidelines.md` route; no project CLAUDE.md; 100 `-sample.html` previews with no path from the UI |
| Clash (contradictory)        | HIGH | Docs say "Phase 1 (now): shared passcode"; code says Supabase-only. No precedence rule declares which wins |

## Since last audit
First run — no prior report in `audits/`.

## What would make your agent wrong-answer you today

1. **Ask "how does auth work here?"** → README says shared passcode + HMAC cookie (Phase 1),
   which is flatly wrong. Passcode was removed in `08b26d5` this morning. An agent reading
   the README will reinstate a gate you deliberately deleted.
2. **Ask "what's left to build for Supabase?"** → SUPABASE_SETUP.md's "Still to wire" section
   lists login UI and Save/My Projects buttons. Both shipped (`a5a55b2`, `324d522`). An agent
   will rebuild them on top of working code.
3. **Ask "set the env vars for deploy"** → README's table tells it to set `PRIVATE_TOOL_PASSCODE`
   and `PRIVATE_TOOL_COOKIE_SECRET`, which nothing reads. `.env.example` is the correct list;
   nothing says so.
4. **Hero variation 9 renders a broken image** — `PERSON_IMG` at `private-content.tsx:1260`
   points at `/private/hero-v9-person.webp`, which is not on disk. Silent 404, no error.
5. **Ask for frontend guidelines** (which you're about to) → the global manual routes to
   `System/frontend/frontend-guidelines.md`; that folder doesn't exist. `System/` contains only
   `core/`. The agent walks into a wall and improvises.

## Findings by check

### Check 1 — Routing integrity
- `System/frontend/frontend-guidelines.md` referenced by global CLAUDE.md — **dead path**, the
  `frontend/` directory does not exist under `System/`. The four `core/*.md` targets all resolve. `[confusion]`
- No project-level `CLAUDE.md` or `.claude/` directory. A fresh session on this repo gets global
  rules plus the stale README, with no project router. `[confusion]`
- No memory index (`MEMORY.md`) for this project — nothing to verify, but also nothing carrying
  the "passcode was intentionally removed" decision forward. `[confusion]`

### Check 2 — Index truth (code catalog vs. `public/private` on disk)
| Direction | Count | Detail |
|---|---|---|
| Referenced by code | 186 | literal `/private/...` paths in `app/` + `lib/` |
| On disk | 320 | files under `public/private/` |
| **Phantom** (referenced, missing) | **1** | `/private/hero-v9-person.webp` — `private-content.tsx:1260` `[poisoning]` |
| **Orphan** (on disk, unreferenced) | **135** | see breakdown below `[bloat]` |

Orphan breakdown: 100 `*-sample.html` preview files, 11 `.webp` assets
(`hero-v1-sample-bg`, `hero-v3-card-1..4`, `hero-v7-*`, `hero-variation-1`), plus whole
subtrees never linked from the app — `ai-academy/` (12), `carousel/` (5), `gpt-image/` (5),
`local/` (2 resort pages: `isla-serena.html`, `lunara-beach-resort.html`).
There are **no dynamic `` `/private/${...}` `` template references anywhere in the codebase**, so
these are genuinely unreachable from the UI, not lazily-constructed URLs. Either the
sample-preview feature was never wired, or it was dropped in the extraction from the portfolio.
Worth confirming before anything moves — they may be intentional manual-open references.

- Variation numbering has real gaps that suggest incomplete catalog entries:
  `before-after-v7` missing entirely (v1–v6, v8 present); `empathy-v9`/`empathy-v10` have
  thumbnails but no `-sample.html`. `[confusion]`

### Check 3 — Freshness (docs vs. code)
No recurring data feeds exist in this project — nothing is scheduled, pulled, or ingested.
The staleness is entirely in the docs layer, which drifted behind the code **within a single day**:

| Doc | Last written | Code moved | Verdict | What's wrong |
|---|---|---|---|---|
| `README.md` | 09:18 | 10:36 | **FROZEN** | Auth section, env table, and deploy steps all describe the deleted passcode gate |
| `SUPABASE_SETUP.md` | 09:18 | 10:36 | **FROZEN** | "App runs today on the passcode gate alone"; "still to wire" lists 2 shipped features |
| `lib/supabase/server.ts:13` | 09:16 | — | DRIFTING | Comment still says "passcode gate alone (Phase 1)" |
| `.env.example` | 10:36 | 10:36 | **FRESH** | Correct — this is the true env list |

- README claims `npm run dev # http://localhost:3100 (or 3000)`, but `package.json` `dev` is
  plain `next dev` → port 3000. SUPABASE_SETUP.md then tells you to allow-list
  `http://localhost:3100` in Supabase redirect URLs — a login redirect that will not match. `[poisoning]`

### Check 4 — Bloat, duplication, organization
- `app/private-content.tsx` — **13,076 lines**, 95% of the 13,812-line codebase. Section catalog,
  variation metadata, prompt templates, and UI all in one file. Every edit loads the whole thing;
  every agent read of it burns enormous context. This is the single biggest structural risk. `[bloat]`
- `.next/` is **247 MB** vs. 12 MB of real assets. Correctly gitignored, so this is disk hygiene
  only — not a context problem. `[bloat]`
- **Root hygiene: clean.** All 11 loose root files are legitimate Next.js config or docs. No scratch,
  no dumps, no stray exports. Nothing to re-home.
- `public/private/local/` holds two full resort landing pages (`isla-serena.html`,
  `lunara-beach-resort.html`, ~120 KB) unreferenced by the builder — leftovers from the portfolio
  extraction. `[confusion]`
- **Intuitiveness check:** walking to "the empathy v3 preview" or "the AI Academy hero section"
  by folder names alone works — `public/private/` is flat but consistently named. Walking to
  "where variation metadata is defined" does **not**: it's line ~1160 of a 13k-line file with no
  index. A human file-explorer trail finds the assets and loses the logic.

### Check 5 — Hygiene and silent failures
- **Secrets: PASS.** `.env.local` matched by `.gitignore:24` (`.env*.local`) and untracked.
  `.env.example` contains names only, no values. No credential files, no PII, no exported data
  in the 344 tracked files.
- **No tests exist.** No `test` or `typecheck` script in `package.json`, no test files, no runner
  installed. Your global rules require "a unit test for every new service function" and
  "run tests and typecheck after every change before saying done" — neither is currently possible.
  The API routes (`funnel-analyze`, `projects`, `projects/[id]`) and RLS behaviour are entirely
  unverified by automation. `[confusion]`
- **No hooks, no CI, no scheduled jobs.** Every check is manual, which is exactly why the docs
  froze two commits behind the code on day one. `[clash]`
- No orphaned agent-memory folders, no empty dirs, no 0-byte files.

### Check 6 — Context placement
- **Situational facts baked into the expertise layer:** README's "Phase 1 (now)" / "Phase 2"
  framing is a point-in-time status written into the file every agent reads first. It has already
  gone stale once, in under two hours. The fix is not a fresher status line — it's removing status
  from the README and letting `.env.example` + the code be the source of truth. `[poisoning]` `[bloat]`
- **Expertise buried in situational stores:** the 10P section definitions, variation naming
  convention (`{section}-v{n}-{role}.webp`), and prompt-construction rules are standing knowledge
  encoded only inside `private-content.tsx`. Nothing at the routing layer says they exist. `[confusion]`
- **No precedence rule anywhere.** README, SUPABASE_SETUP.md, `.env.example`, and the code all
  make claims about auth and env vars, and three of the four disagree. Nothing declares a winner. `[clash]`
- Spot-checked facts across stores: *auth mechanism* → 3 stores, 2 ages, contradictory.
  *Env var list* → 2 stores, contradictory. *Dev port* → 2 stores, contradictory.
  *Supabase schema* → 1 store (`0001_init.sql`), consistent. Three of four key facts are live clashes.

## Questions for you

1. **The 100 `-sample.html` files and the `ai-academy/` + `carousel/` + `gpt-image/` subtrees** —
   were these meant to be linked as live previews in the builder, or are they reference material
   you open by hand? The answer decides whether this is a missing feature or an archive move.
2. **`public/private/local/`** (two resort pages) — portfolio leftovers, or still in use?
3. **`before-after-v7`** — deliberately skipped, or a gap to fill?
4. **`System/frontend/frontend-guidelines.md`** — did this folder ever exist, or is the global
   CLAUDE.md pointing at something you planned but never built? Relevant immediately, since
   frontend design is the next thing on your list.

## Fix list (batched, await approval)

**Batch A — security + dead capabilities (do first)**
- Nothing security-critical. Secrets handling is clean; no action needed.
- Fix the dead route: either create `System/frontend/frontend-guidelines.md` or repoint the
  global CLAUDE.md line at what actually exists.

**Batch B — routing + index reconciliation**
- Rewrite `README.md`: delete the passcode env rows and the Phase 1/Phase 2 auth narrative,
  point the env table at `.env.example` as source of truth, fix the dev port to 3000.
- Rewrite `SUPABASE_SETUP.md`: move Save/My Projects and login UI from "still to wire" to
  "wired"; fix the `3100` redirect allow-list instruction.
- Fix the `lib/supabase/server.ts:13` comment.
- Fix or remove `/private/hero-v9-person.webp` (add the asset, or repoint the constant).
- Add a project `CLAUDE.md`: what this app is, the 10P section catalog location, the asset
  naming convention, and one precedence line — "`.env.example` and the code are the source of
  truth for configuration; docs describe, they don't define."

**Batch C — structure + data catch-up**
- Split `app/private-content.tsx`: section catalog → `lib/sections/`, prompt templates →
  `lib/prompts/`, UI stays. This is the highest-leverage change in the repo.
- Decide the orphan assets (question 1) — wire the sample previews or move them to an archive folder.
- Fill the `before-after-v7` / `empathy-v9`/`v10` catalog gaps, or document them as intentional.

**Batch D — durability (so it can't silently freeze again)**
- Add `"typecheck": "tsc --noEmit"` and a test runner to `package.json`, with tests for the three
  API routes and RLS scoping (user B cannot read user A's projects — currently unverified).
- Add a pre-commit or PostToolUse hook that flags README/SUPABASE_SETUP.md edits when auth or
  env code changes, so docs can't drift two commits behind again.
